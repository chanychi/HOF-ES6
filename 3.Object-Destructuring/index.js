const person = {
  firstName: 'Walter',
  lastName: 'White',
  alias: 'Heisenberg',
  age: 50
};

const {firstName, lastName, alias, age} = person;
console.log(firstName) // same as person.firstName
console.log(age) // same as person.age