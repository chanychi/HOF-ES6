const person = {
  name: 'Julia',
  title: 'TM'
}
console.log(person.name + ' we\'ll miss you') //print "Julia we'll miss you"
console.log(`${person.name} we'll miss you`) //print "Julia we'll miss you"

console.log(
`Where's
the
${person.title}`) //prints out each line without using \n